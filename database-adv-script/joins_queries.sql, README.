SELECT 
    booking.booking_id,
    booking.property_id,
    booking.start_date,
    user.user_id,
    user.first_name,
    user.email
FROM 
    booking
INNER JOIN 
    user
ON 
    booking.user_id = user.user_id;
SELECT 
    booking.booking_id,
    booking.property_id,
    booking.start_date,
    user.user_id,
    user.first_name,
    user.email
FROM 
    booking
INNER JOIN 
    user
ON 
    booking.user_id = user.user_id;

e
